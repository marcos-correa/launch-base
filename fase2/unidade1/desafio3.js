const empresas = {
        name:"Rocketseat",
        color:"Roxo",
        address:{
            tipo:"Rua",
            logradouro:"Guilherme Gembala",
            number:230
        }
    }

console.log(`A empresa ${empresas.name} está localizada na ${empresas.address.tipo} ${empresas.address.logradouro}, ${empresas.address.number}`)